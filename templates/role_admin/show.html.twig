{% extends 'base.html.twig' %}

{% block title %}RoleAdmin{% endblock %}

{% block body %}
    <h1>RoleAdmin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ role_admin.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ role_admin.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_role_admin_index') }}">back to list</a>

    <a href="{{ path('app_role_admin_edit', {'id': role_admin.id}) }}">edit</a>

    {{ include('role_admin/_delete_form.html.twig') }}
{% endblock %}
